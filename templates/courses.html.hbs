<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Courses</title>
  </head>
  <body>

    <section id="hello">
      <h1>Courses</h1>
      New courses
      <ul>
        {{#each courses}}
          <li>Title: {{this.title}}</li>
          <li>Body: {{this.description}}</li>
        {{else}}
          <p> No courses yet</p>
        {{/each}}
      </ul>
    </section>

  </body>
</html>